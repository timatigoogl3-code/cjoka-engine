# cjoka Engine — TODO (рабочий спринт v0.50 FLAGSHIP)

Текущая версия: **v0.48 BETA**. Следующий релиз: **v0.50** (самые крутые фичи).
Полный план версий — в `ROADMAP.md`. Статус фич по аудиту: ✅ работает / ⚠️ написано, но выключено / ❌ не заматчено или нет.

---

## Этап A — Производительность открытого мира (блокирует всё остальное)
**Цель:** город из реальных мешей должен поехать и не сожрать 6 ГБ VRAM.

### A1. Заматчить ClusterLOD в рендер-путь
- [x] Найти путь отрисовки меши в `Renderer.cpp` (интегрирован `MeshClusters::DrawInstanced`).
- [x] В `MeshClusters.cpp` `SelectLODs(mvp, model, camPos, fovY, screenH, errorThresholdPx)` — вызов перед draw; получение списка видимых кластеров.
- [x] Рисовать кластеры через **MultiDraw** (инстанс-бакеты по 128 треугольников), а не весь меш.
- [x] Проверить, что флаг `MeshRenderer.clusterLOD` (редактор Alt+6 / инспектор) реально меняет кадр.
- [x] **Риск:** T-швы при упрощении — `ComputeLockedVertices` гарантирует отсутствие щелей.
- [x] **Приёмка:** на удалённых зданиях треугольников меньше, артефактов нет, фпс растёт.

### A2. Инстансинг общих мешей
- [x] `kLitInstancedVS` / `kLitInstancedFS` подключены — батч одинаковых мешей в 1 draw call.
- [x] Хранить меш/материал по `shared_ptr` (не копировать вершины при спавне из редактора/ассет-браузера).
- [x] Группировать меши по (mesh+material) в `Systems.h` / `Batcher.cpp` (с `aInstanceAlbedo`/`aInstanceEmissive`).
- [x] **Приёмка:** одинаковые здания/фонари = 1 draw call в стате (см. профайлер F3).

### A3. Стриминг мира
- [x] `ChunkManager`: сетка чанков, индекс чанка → список entity/мешей.
- [x] В `onUpdate` камеры: грузить чанки в радиусе `R`, выгружать за `R+margin`.
- [x] Спавн/удаление entity в ECS при входе/выходе из радиуса; **fade pop-in** (alpha/distance).
- [x] Лимит одновременно загруженного по VRAM (связь с VRAM-гардом 0.50a).
- [x] **Приёмка:** город не влезает целиком, но едет плавно; VRAM стабильна.

---

## Этап B — Next-Gen Lighting (визуал, ради которого смотрят)
**Цель:** картинка, которую игрок хочет разглядывать.

### B1. Включить и отладить выключенные эффекты
- [x] TAA (`RenderPipeline.h` `taa=true`): YCoCg clamping, Halton subpixel jitter, motionFade без гостинга.
- [x] GTAO (`gtao=true`): 5x5 cross-bilateral blur с depth & spatial weights, убран banding.
- [x] Volumetric Fog (`volumetricFog=true`): атмосферный туман/рассеяние света в дождь.
- [x] z-prepass (`zPrepass=false`): рендер глубины для минимизации овердрава.
- [x] **Приёмка:** без мыла/артефактов, фпс стабилен на RTX 3050 (контроль профайлером).

### B2. VXGI — динамический GI
- [x] 3D Clipmap (воксельная сетка вокруг камеры) + вокселизация геометрии.
- [x] Cone tracing: 6 диффузных + 1 зеркальный конус от поверхности → мягкий небейкнутый отскок.
- [x] Интеграция как pass в `RenderPipeline` и `Systems.h`.
- [x] **Приёмка:** ночью свет фонаря бьёт по стенам/асфальту, без RT-ядер.

### B3. Ночь / Дождь / Отражения
- [x] **Wetness-материал**: понижение `roughness` и затемнение `albedo` под дождём (`uWetness`).
- [x] SSR (`ssr=true`) — правильное декодирование view-space нормалей, hi-z raymarch с бинарным уточнением.
- [x] Связка с Weather API (`WorldEnvironment`): интенсивность дождя модулирует wetness.
- [x] **Приёмка:** под дождём асфальт зеркально отражает свет и неон.

---

## Этап C — Baked Mixed Lighting (статика города)
**Цель:** статику запечь дёшево, динамике дать живые тени + отскок.

### C1. Lightmap UV2
- [ ] Генерация второго UV-канала без наложений (планарно/воксельно или из FBX, если есть).
- [ ] `FBXLoader`/`MeshLoader`: хранить UV2 отдельно от основного UV.
- [ ] **Приёмка:** бейк не плывёт из-за совмещённых островов.

### C2. Бейкер
- [ ] Офлайн-просчёт теней/indirect от источников → lightmap-текстура (свой или либа).
- [ ] Привязка lightmap к чанку (грузится вместе с геометрией, см. A3).
- [ ] **Приёмка:** статичные здания получают мягкие тени/отскок без GPU-стоимости.

### C3. Mixed blend
- [ ] Статика рисуется с lightmap; динамика (машины/игрок) — CSM (уже есть).
- [ ] Бленд по краю, чтобы движущийся объект не висел без тени на бейкнутом асфальте.
- [ ] **Приёмка:** шва между бейком и динамикой нет.

### C4. Light Probes
- [ ] Сэмплы запечённого GI в точках мира; динамика (NPC/машины) ловит свет города.
- [ ] Интерполяция по позиции entity в шейдере/рантайме.
- [ ] **Приёмка:** машины не «плоские» на фоне пропечённого города.

### C5. Бейк ночи
- [ ] Запечь отражённый свет от неона/фонарей (emissive → lightmap) — город светится дёшево ночью.
- [ ] **Приёмка:** ночной город с глубиной, без RT, дёшево по фпс.

> **Связь с Shader Graph (0.60):** материал должен стать генерируемым (носить опц. сгенерённый фрагмент).
> Заложить хук здесь, на этапе C, чтобы граф потом встал без переписывания.

---

## Этап D — Универсальный геймплей-слой (v0.54, движок)
> Миссии/сюжет — НЕ в движке. Движок даёт дженерик-инфраструктуру, игра собирает миссии поверх.
### D1. Trigger / Event система
- [ ] ECS-компоненты зон/триггеров; ивенты коллизий/входа-выхода.
- [ ] Шина ивентов (подписка/эмит). **Приёмка:** триггер запускает ивент.
### D2. World State Save / Load
- [ ] Сериализация состояния ECS + прогресс (база сериализации уже есть).
- [ ] **Приёмка:** save→load восстанавливает мир без потерь (детерминизм из 0.50a).
### D3. Скриптинг API
- [ ] Доступ к ивентам/триггерам из `ScriptableEntity`.
- [ ] **Приёмка:** скрипт реагирует на триггер/ивент.
### D4. Blackboard
- [ ] Мировые переменные/флаги/счётчики для логики квестов в слое игры.
- [ ] **Приёмка:** скрипт читает/пишет флаг, влияющий на мир.

---

## Этап E — Asset Pipeline / Моддинг (универсальность + Steam)  **[ОПЦИОНАЛЬНО]**
> **НЕ блокирует релиз.** Базовое (ассеты вне бинаря) уже выполнено. `.pkg`/VFS — полировка под моды/Steam, делается когда удобно.
- [ ] **Гарантия внешних ассетов**: проверить, что стендалон-билдер НЕ эмбедит меши/текстуры/сцены в exe (копия/symlink `assets/`).
- [ ] **VFS / mount-слой**: `AssetManager` резолвит пути через порядок `loose assets/` > `mods/<name>/*.pkg` > `assets/*.pkg` (база).
- [ ] **.pkg по категориям**: упаковать ассеты в `assets/textures.pkg`, `meshes.pkg`, `audio.pkg`, `scenes.pkg`, `materials.pkg` (простой формат: заголовок+оглавление+блобы, или ZIP-совместимый).
- [ ] **Приёмка моддинга**: файл/пак в `mods/test/` переопределяет базовый ассет без правки оригинала.
- [ ] **Steam-готовность**: каждый `.pkg` — отдельный файл депо → правка текстур редownload только `textures.pkg` (дельта).
- [ ] **Shader Graph (0.60)**: сгенерённые материал-шейдеры — с диска/рантайм-компиляция, не эмбед.

## Этап F — Текстурный пайплайн (BCn-компрессия)  **[рекомендуется, влияет на перф]**
> Контейнер `.pkg` — опционален; BCn-компрессия текстур — практически обязательна для VRAM/бандвидта на 3050.
- [ ] **Офлайн-конвертация** ассетов в BCn (билд-скрипт): albedo→BC1, albedo+alpha→BC3, нормали→BC5, RMAO-упаковка→BC4/BC5, цвет/эмиссив→BC7.
- [ ] **Загрузка `.dds(BCn)` напрямую** (без декомпрессии в RGBA8); запрет сырых PNG/JPG в проде.
- [ ] **Приёмка:** текстура 2K RGBA8 (16 МБ) → BC1 (~2 МБ); VRAM-профиль (0.50a) показывает падение.

## Порядок исполнения в спринте
A1 → A2 → A3 → B1 → B2 → B3 → C1..C5 → (D вынесен в v0.54).
После A3+B1 картинка уже «та, что разглядывают»; C и B2 добавляют глубину/ночь.

## Разрыв до цели (честно)
- Техника рендера до «красивого дефолтного вида»: ~65% (остаток — VXGI + вкл. TAA/GTAO + Mixed-bake).
- Открытый город едет: блокируется A1/A3 (ClusterLOD + стриминг).
- «Вторая GTA V»: не цель; курируемый район + 13 миссий + сюжет 2-4ч — реалистично.
